--- a/Concepts/example.md
+++ b/Concepts/example.md
@@ -1,27 +1,28 @@
 <!SLIDE>
-# Resource Declarations
-## Resources are managed in terms of *attributes*.
+# Resource Attributes
+## Resources are managed by setting *attributes*.
 
-* Instruct Puppet to manage a **package**:
+* Set attributes for a **user** resource:
 
 .break text
 
     @@@ Puppet
-    package { 'openssh':
-      ensure => present,
+    user { 'elvis':
+      ensure => present,    # The 'ensure' attribute is set to 'present'
+      home   => '/graceland', # The 'home' attribute set to '/graceland'
     }
 
-* Instruct Puppet to manage a **user**:
+* Set attributes for a **package** resource:
 
 .break text
 
     @@@ Puppet
-    user { 'elvis':
-      ensure => absent,
+    package { 'mysql':
+      ensure          => installed,
+      source          => 'N:/packages/mysql-5.5.16-winx64.msi',
+      install_options => [ '/S', { 'INSTALLDIR' => 'C:\mysql-5.5' } ],
     }
 
-### Attributes describe the state that Puppet should converge the resource to.
-
 ~~~SECTION:notes~~~
 
 Resource attributes are simply a list of things that Puppet should care about

--- a/Concepts/abstraction.md
+++ b/Concepts/abstraction.md
@@ -3,12 +3,8 @@
 ## Resources in Puppet are abstracted from underlying providers.
 
     @@@ Puppet
-    package { 'ssh':
+    package { 'wget':
       ensure => present,
-      name   => $::operatingsystem ? {
-        'RedHat' => 'openssh',
-        'Ubuntu' => 'ssh',
-      },
     }
 
 This resource declaration will use different tools on different platforms:
@@ -18,14 +14,14 @@ This resource declaration will use different tools on different platforms:
 .break text
 
     @@@ Shell
-    yum install openssh
+    yum install wget
 
 * Debian family
 
 .break text
 
     @@@ Shell
-    apt-get install ssh
+    apt-get install wget
 
 ~~~SECTION:notes~~~
 
